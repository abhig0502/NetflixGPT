import React from 'react'
import Header from './Header';

const LoginHeader = () => {
  return (
    <Header>
    <div>
      <button>Sign In</button>
    </div>
    </Header>
  )
}

export default LoginHeader;
